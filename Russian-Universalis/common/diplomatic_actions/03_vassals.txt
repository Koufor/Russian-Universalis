annexationaction = {
	# Ливонская конфедерация - конфедераты должны быть усмирены и иметь флаг "liv_bishop_can_integrate"
	condition = { 
		tooltip = requirements_for_livonian  
		potential = {
			FROM = { 
				is_subject_of = ROOT
				is_subject_of_type = livonian_vassal
				NOT = { has_country_flag = liv_bishop_can_integrate }
			}
		}
		allow = {
			FROM = {
				is_subject_of = ROOT
				is_subject_of_type = livonian_vassal
				has_country_flag = liv_bishop_can_integrate
				has_opinion = {
					who = ROOT
					value = 190
				}
			}
		}
	}
	
	# Требования по интеграции для всех вассалов
	condition = { 
		tooltip = triple_integration_needed_1
		potential = {
			FROM = {
				vassal_of = ROOT
				NOT = { check_variable = { which = PreSent_integration_ADM value = 100 } }
			}
		}
		allow = {
			FROM = {
				vassal_of = ROOT
				check_variable = { which = PreSent_integration_ADM value = 100 }
			}
		}	
	}
	condition = { 
		tooltip = triple_integration_needed_2
		potential = {
			FROM = {
				vassal_of = ROOT
				NOT = { check_variable = { which = PreSent_integration_DIP value = 100 } }
			}
		}
		allow = {
			FROM = {
				vassal_of = ROOT
				check_variable = { which = PreSent_integration_DIP value = 100 }
			}
		}	
	}
	condition = { 
		tooltip = triple_integration_needed_3
		potential = {
			FROM = {
				vassal_of = ROOT
				NOT = { check_variable = { which = PreSent_integration_MIL value = 100 } }
			}
		}
		allow = {
			FROM = {
				vassal_of = ROOT
				check_variable = { which = PreSent_integration_MIL value = 100 }
			}
		}	
	}
	
	# Стандартное требование
	condition = { 
		tooltip = ANNEX_TOO_LOW
		potential = {
			FROM = {
				vassal_of = ROOT
			}
		}
		allow = {
			FROM = {
				has_opinion = {
					who = ROOT
					value = 190
				}
			}
		}
	}
}